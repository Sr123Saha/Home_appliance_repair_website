Home Service: Система учета заявок на ремонт
Веб-приложение для управления сервисным центром по ремонту климатической техники. Позволяет регистрировать заявки, назначать мастеров и отслеживать статус выполнения работ.

🛠 Технологии
Backend: Python / Flask

Database: SQLite

Frontend: Jinja2 (HTML шаблоны)

Дополнительно: QR-code generation, Pandas (для импорта данных)

📂 Структура проекта
web_app.py — основное Flask-приложение и роуты.

bd_.py — скрипт инициализации базы данных и импорта данных из CSV.

run_web.py — утилита для проверки зависимостей и запуска сервера.

inputData*.csv — исходные данные для наполнения БД (пользователи, заявки, комментарии).

testsss.py — тесты для проверки авторизации и доступности страниц.

🚀 Быстрый запуск
Установите зависимости:

Bash

pip install flask qrcode pillow pandas pytest
Подготовьте базу данных: Запустите скрипт для создания home_service.db и импорта данных из CSV:

Bash

python bd_.py
Запустите приложение:

Bash

python run_web.py
Приложение будет доступно по адресу: http://127.0.0.1:5000

🔑 Тестовые данные для входа
Для тестирования можно использовать данные из inputDataUsers.csv:

Менеджер: kasoo / root

Мастер: login1 / pass1

Заказчик: login2 / pass2

🧪 Тестирование
Для запуска автоматических тестов используйте:

Bash

pytest testsss.py
Заключение и выводы
В ходе разработки была создана рабочая экосистема для управления сервисными заявками.

Основные результаты:

Эффективное хранение: Реализована реляционная структура таблиц, связывающая клиентов, мастеров и их комментарии к заявкам.

Безопасность: Доступ к управлению заявками и статистике ограничен с помощью декораторов авторизации (login_required, manager_required).

Гибкость: Система поддерживает быструю загрузку данных через CSV, что позволяет легко переносить проект между средами.

Интерактивность: Реализована динамическая генерация QR-кодов, ведущих на форму обратной связи, что повышает уровень сервиса.

Проект готов к эксплуатации в качестве MVP и имеет потенциал для масштабирования (переход на PostgreSQL, добавление API и расширенной аналитики).